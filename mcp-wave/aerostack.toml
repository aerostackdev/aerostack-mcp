name = "mcp-wave"
description = "Manage free accounting with Wave — invoices, customers, products, transactions, and chart of accounts via GraphQL from AI."
category = "Finance"
tags = ["accounting", "invoicing", "free", "small-business", "graphql"]
env = ["WAVE_ACCESS_TOKEN", "WAVE_BUSINESS_ID"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
