name = "mcp-sendgrid"
description = "MCP server for SendGrid — email sending, templates, contacts, stats, and sender management"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
