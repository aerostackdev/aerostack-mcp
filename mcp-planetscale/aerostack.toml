name = "mcp-planetscale"
description = "MCP server for PlanetScale — manage MySQL databases and deploy requests"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
