name = "mcp-lattice"
description = "People management via Lattice — manage goals, 1:1s, reviews, and employee feedback."
category = "HR & Payroll"
tags = ["lattice", "hr", "performance", "reviews", "goals"]
env = ["LATTICE_API_KEY"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
