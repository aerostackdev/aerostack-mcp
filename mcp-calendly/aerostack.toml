name = "mcp-calendly"
description = "MCP server for Calendly — users, event types, scheduled events, scheduling links, webhooks"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
