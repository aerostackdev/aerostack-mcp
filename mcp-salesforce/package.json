{
    "name": "mcp-salesforce",
    "version": "1.0.0",
    "private": true,
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20241224.0",
        "@types/node": "^25.5.0",
        "typescript": "^5.6.0",
        "vitest": "^2.0.0",
        "wrangler": "^3.0.0"
    },
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "test:watch": "vitest"
    }
}
