name = "mcp-klaviyo"
description = "MCP server for Klaviyo — profiles, lists, events, campaigns, flows, and templates"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

[secrets]
KLAVIYO_API_KEY = "Your Klaviyo Private API key (Account → Settings → API Keys)"
