name = "mcp-langsmith"
description = "LLM observability and tracing via LangSmith — list projects, query traces, view feedback, and monitor AI runs."
category = "AI / ML"
tags = ["langsmith", "langchain", "observability", "tracing", "llm"]
env = ["LANGSMITH_API_KEY"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
