name = "mcp-viber"
description = "Viber messaging API — send messages to users, broadcast to subscribers, and query bot account info."
category = "Communication"
tags = ["viber", "messaging", "chat", "bot", "asia"]
env = ["VIBER_AUTH_TOKEN"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
