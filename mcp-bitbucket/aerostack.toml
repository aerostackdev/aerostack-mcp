name = "mcp-bitbucket"
description = "Manage Bitbucket repositories, branches, pull requests, commits, and pipelines with natural language."
category = "Developer Tools"
tags = ["bitbucket", "git", "version-control", "ci-cd", "devops"]
env = ["BITBUCKET_TOKEN"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
