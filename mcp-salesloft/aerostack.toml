name = "mcp-salesloft"
description = "Salesloft sales engagement platform — manage people, cadences, accounts, and call/email activities."
category = "CRM"
tags = ["salesloft", "sales-engagement", "cadences", "outreach", "crm"]
env = ["SALESLOFT_API_KEY"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

[secrets]
SALESLOFT_API_KEY = "Your Salesloft API key — found in Salesloft Settings → API → API Keys"
