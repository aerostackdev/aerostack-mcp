name = "mcp-contentful"
description = "Connect your Contentful CMS to AI — manage entries, assets, content types, and spaces with natural language."
category = "CMS"
tags = ["contentful", "cms", "content-management", "headless-cms", "api"]
env = ["CONTENTFUL_ACCESS_TOKEN", "CONTENTFUL_SPACE_ID"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
