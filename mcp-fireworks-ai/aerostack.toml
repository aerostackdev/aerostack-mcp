name = "mcp-fireworks-ai"
description = "Fast LLM inference via Fireworks AI — generate text, run chat completions, and access fine-tuned models at speed."
category = "AI / ML"
tags = ["fireworks", "llm", "inference", "generative-ai", "language-models"]
env = ["FIREWORKS_API_KEY"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
