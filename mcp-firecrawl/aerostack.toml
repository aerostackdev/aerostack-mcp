name = "mcp-firecrawl"
description = "Scrape web pages, crawl entire sites, extract structured data, and search the web — AI-native web data extraction for RAG pipelines."
category = "Data"
tags = ["firecrawl", "web-scraping", "crawl", "rag", "extraction", "search"]
env = ["FIRECRAWL_API_KEY"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
