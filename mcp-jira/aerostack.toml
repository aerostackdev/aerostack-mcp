name = "mcp-jira"
description = "MCP server for Jira — search issues, create tasks, and manage workflows"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
