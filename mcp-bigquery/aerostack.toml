name = "mcp-bigquery"
description = "Run SQL queries, list datasets and tables, inspect schemas, and export results from Google BigQuery — AI-native data warehouse access."
category = "Database"
tags = ["google", "bigquery", "database", "sql", "analytics", "data-warehouse"]
env = ["GOOGLE_SERVICE_ACCOUNT_JSON", "GOOGLE_PROJECT_ID"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
