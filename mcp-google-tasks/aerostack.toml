name = "mcp-google-tasks"
description = "Manage Google Tasks — create, update, and organize task lists and tasks with full CRUD operations."
category = "Productivity"
tags = ["google", "tasks", "todo", "productivity", "gtasks"]
env = ["GOOGLE_TASKS_ACCESS_TOKEN"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
