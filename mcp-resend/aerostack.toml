name = "mcp-resend"
description = "MCP server for Resend — send transactional emails and manage domains"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
