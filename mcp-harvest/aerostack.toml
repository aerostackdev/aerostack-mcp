name = "mcp-harvest"
description = "Track time and manage invoicing with Harvest — time entries, projects, clients, tasks, reports, and invoices from AI."
category = "Finance"
tags = ["time-tracking", "invoicing", "projects", "billing", "reports"]
env = ["HARVEST_ACCESS_TOKEN", "HARVEST_ACCOUNT_ID"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
