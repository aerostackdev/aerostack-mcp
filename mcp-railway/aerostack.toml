name = "mcp-railway"
description = "MCP server for Railway — manage projects, services, deployments, and logs"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
