name = "mcp-notion"
description = "MCP server for Notion — search pages, manage databases, and create content"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
