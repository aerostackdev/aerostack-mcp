name = "mcp-mongodb"
description = "Query collections, insert documents, and run aggregation pipelines on your MongoDB Atlas database — AI-native document database access."
category = "Database"
tags = ["database", "mongodb", "nosql", "document", "atlas"]
env = ["MONGODB_APP_ID", "MONGODB_API_KEY", "MONGODB_CLUSTER"]
main = "src/index.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]
