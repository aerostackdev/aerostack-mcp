name = "mcp-cloudflare"
description = "MCP server for Cloudflare — manage Workers, KV, R2, D1, and analytics"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
