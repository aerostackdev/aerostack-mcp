name = "mcp-dropbox"
description = "List, upload, download, search, move, and manage files and folders in Dropbox — AI-native cloud storage for any agent."
category = "Storage"
tags = ["dropbox", "storage", "files", "folders", "cloud", "sync"]
env = ["DROPBOX_ACCESS_TOKEN"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
