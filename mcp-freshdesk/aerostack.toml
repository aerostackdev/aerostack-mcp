name = "mcp-freshdesk"
description = "Create tickets, manage contacts, and resolve support issues faster — give your AI agents full Freshdesk access."
category = "Customer Support"
tags = ["support", "helpdesk", "tickets", "crm", "freshdesk"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

[vars]
FRESHDESK_API_KEY = "Your Freshdesk API key (Profile Settings → API Key)"
FRESHDESK_DOMAIN = "Your Freshdesk subdomain (e.g. 'acme' for acme.freshdesk.com)"
