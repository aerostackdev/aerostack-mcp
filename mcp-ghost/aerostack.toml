name = "mcp-ghost"
description = "MCP server for Ghost — manage posts, pages, and members via Ghost Admin API with JWT authentication"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
