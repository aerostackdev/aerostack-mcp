name = "mcp-vimeo"
description = "Video hosting and management via Vimeo — upload videos, manage albums, update metadata, and get analytics."
category = "Media"
tags = ["vimeo", "video", "hosting", "media", "streaming"]
env = ["VIMEO_ACCESS_TOKEN"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
