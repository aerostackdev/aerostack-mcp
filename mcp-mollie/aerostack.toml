name = "mcp-mollie"
description = "Mollie payment gateway — manage payments, customers, subscriptions, and refunds. Popular in Europe with iDEAL and SEPA support."
category = "Payments"
tags = ["mollie", "payments", "europe", "subscriptions", "ideal", "sepa"]
env = ["MOLLIE_API_KEY"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

[secrets]
MOLLIE_API_KEY = "Your Mollie API key — test keys start with test_, live keys start with live_"
