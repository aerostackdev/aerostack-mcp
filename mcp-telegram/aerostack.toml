name = "mcp-telegram"
description = "MCP server for Telegram — send/receive messages, manage groups, members, roles, and bots"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
