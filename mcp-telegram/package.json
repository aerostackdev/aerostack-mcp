{
    "name": "mcp-telegram",
    "version": "1.0.0",
    "private": true,
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20241224.0",
        "typescript": "^5.6.0",
        "wrangler": "^3.0.0",
        "vitest": "^2.0.0"
    },
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "test:watch": "vitest",
        "test:e2e": "TELEGRAM_BOT_TOKEN=$TELEGRAM_BOT_TOKEN vitest run"
    }
}
