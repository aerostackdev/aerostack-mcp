name = "mcp-browserbase"
description = "Cloud browser automation via Browserbase — create sessions, run Playwright scripts, capture screenshots."
category = "Developer Tools"
tags = ["browserbase", "browser", "playwright", "automation", "scraping"]
env = ["BROWSERBASE_API_KEY"]
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
